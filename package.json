{
  "name": "bruno-menezes-professional-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run validate && astro check && astro build",
    "preview": "astro preview",
    "check": "npm run validate && astro check",
    "validate": "node scripts/validate-content.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.6",
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.2.10",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
